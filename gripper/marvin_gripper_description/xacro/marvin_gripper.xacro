<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">
  <xacro:arg name="collider" default="convex"/>
  <xacro:property name="collider_type" value="$(arg collider)"/>

  <xacro:include filename="$(find marvin_gripper_description)/xacro/components/flange.xacro"/>
  <xacro:include filename="$(find marvin_gripper_description)/xacro/components/marvin_gripper.xacro"/>

  <!-- Tianji Marvin gripper (straight flange) with camera. -->
  <xacro:macro name="marvin_gripper" params="name kinematic_only:=false">
    <xacro:marvin_gripper_flange name="${name}"/>
    <xacro:marvin_gripper_component name="${name}" kinematic_only="${kinematic_only}"/>
  </xacro:macro>
</robot>
